import { Routes } from '@angular/router';
import { HomePageComponent } from './views/public/pages/home-page/home-page.component';

export const routes: Routes = [{ path: '', component: HomePageComponent }];
