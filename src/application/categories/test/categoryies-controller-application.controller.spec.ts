import { Test, TestingModule } from "@nestjs/testing";
import { CategoryiesApplicationController } from "../categories-application.controller";

/*describe("CategoryiesControllerApplicationController", () => {
  let controller: CategoryiesApplicationController;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      controllers: [CategoryiesApplicationController],
    }).compile();

    controller = module.get<CategoryiesApplicationController>(
      CategoryiesApplicationController,
    );
  });

  it("should be defined", () => {
    expect(controller).toBeDefined();
  });
});
*/
